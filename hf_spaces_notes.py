# ── Hugging Face Spaces entry point ──────────────────────────────────────────
# HF Spaces with Streamlit SDK just needs this file to be named app.py
# and requirements.txt to be present at the repo root.
# The main app is already named app.py — this comment block is for documentation.
#
# Deployment steps (see README for full guide):
#   1. Create a new Space at huggingface.co/new-space
#   2. Choose SDK: Streamlit
#   3. Push this entire repo to the Space's git remote
#   4. Add GROQ_API_KEY as a Secret in Space Settings → Variables and Secrets
#   5. The app will build automatically on every push
